# Token-free npm config for the Astro target.
#
# Unlike www/.npmrc, this does NOT declare the FontAwesome private registry
# (@fortawesome:registry) or require FONTAWESOME_NPM_AUTH_TOKEN — the Astro site
# uses Fontsource packages instead, so `pnpm install` needs no install-time secret.
auto-install-peers=true
